{
  "name": "mpc-js-web",
  "version": "1.2.0",
  "description": "A javascript client library for the Music Player Daemon - web browser package",
  "files": [
    "lib",
    "umd",
    "README.md"
  ],
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "rm -Rf lib umd && npm run-script build",
    "reinstall": "rimraf node_modules package-lock.json && npm install",
    "clean": "rimraf out vscode-test-explorer-*.vsix",
    "build": "tsc && mkdir -p umd && webpack --entry ./lib/mpc.js --optimize-minimize --output-library MPC --output-library-target umd --output-path umd --output-filename mpc.min.js",
    "watch": "tsc -w",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": [
    "mpd"
  ],
  "author": "Holger Benl <hbenl@evandor.de>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hbenl/mpc-js-web.git"
  },
  "homepage": "https://github.com/hbenl/mpc-js-web",
  "bugs": {
    "url": "https://github.com/hbenl/mpc-js-web/issues"
  },
  "dependencies": {
    "mpc-js-core": "^1.2.0",
    "base64-js": "^1.2.1",
    "@types/base64-js": "^1.2.5",
    "text-encoder-lite-module": "github:hbenl/TextEncoderLiteModule"
  },
  "devDependencies": {
    "rimraf": "^2.6.2",
    "typescript": "~2.4.2",
    "webpack": "^3.4.1"
  }
}
